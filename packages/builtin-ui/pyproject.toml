[project]
name = "media-finder-builtin-ui"
version = "0.1.0"
description = "Built-in Jinja and HTMX interface for Media Finder"
requires-python = ">=3.13,<3.14"
license = "MIT"
dependencies = [
  "babel>=2.17,<3",
  "fastapi>=0.116,<1",
  "jinja2>=3.1,<4",
  "media-finder-control-contracts",
  "uvicorn>=0.35,<1",
]

[dependency-groups]
test = [
  "httpx>=0.28,<1",
  "playwright>=1.55,<2",
  "pytest>=9.0.3,<10",
]

[project.scripts]
media-finder-ui-dev = "media_finder_builtin_ui.dev:main"

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/media_finder_builtin_ui"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers"
filterwarnings = [
  "error",
  "ignore:Using `httpx` with `starlette\\.testclient` is deprecated; install `httpx2` instead\\.:starlette.exceptions.StarletteDeprecationWarning:fastapi\\.testclient",
]
