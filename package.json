{
  "name": "media-finder-tooling",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "assets:build": "node scripts/build-assets.mjs",
    "py:format": "uv run ruff format --check .",
    "py:lint": "uv run ruff check .",
    "py:type": "uv run mypy",
    "py:test": "uv run pytest",
    "spec:validate": "openspec validate bootstrap-media-finder --strict",
    "spec:list": "openspec list"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.9.0",
    "axe-core": "4.11.0",
    "htmx.org": "2.0.10"
  }
}
