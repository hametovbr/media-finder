interface:
  display_name: "Add Release Provider"
  short_description: "Add a conforming torrent release module"
  default_prompt: "Use $adding-release-provider to add a release provider with complete contracts and tests."
