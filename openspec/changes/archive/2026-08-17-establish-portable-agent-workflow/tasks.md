## 1. Freeze the Skill Contract and Baselines

- [x] 1.1 Add a focused RED architecture test for the seven missing cross-cutting skill directories, matching frontmatter names, required Codex metadata, project routing, absence of device-local paths and hard-coded product-release literals in manually maintained skills, and continued presence of the six generated OpenSpec plus four existing domain skills; keep behavioral assertions out of this mechanical test.
- [x] 1.2 Write `docs/agent-skills.md` with explicit/inferred/research-only historical skill classifications, architecture evolution, user interventions, source-name/version provenance, and reusable pressure records containing prompt, available instruction sources, run classification, baseline choice/rationalization, expected and forbidden decisions, post-skill evidence, and causal limitations.
- [x] 1.3 Record historical observed RED evidence for the Docker shell-parser and other applicable failures, then run fresh no-target-skill controls for implementation, debugging, review, contract, verification/release, and skill maintenance; label inherited global/project guidance, record already-correct controls without calling them RED, and never tune a scenario merely to force failure.

## 2. Add Cross-Cutting Skills One at a Time

- [x] 2.1 Add the version- and incident-independent `making-pragmatic-media-finder-decisions` plus `agents/openai.yaml`, rerun the Docker-verifier, independent-UI, and second-provider scenarios to GREEN as examples, and prove any architecture-rung escalation stops for OpenSpec update and renewed approval.
- [x] 2.2 Add `developing-media-finder-changes` plus metadata, rerun the Manual-capability and parallel-legacy-path scenarios to GREEN, and prove every RED test traces to an approved requirement or reproduced approved-behavior defect.
- [x] 2.3 Add `debugging-media-finder-failures` plus metadata, rerun the Windows-only uv path and CI-environment scenarios to GREEN, and prove the result distinguishes product defects, environment limitations, and stale worktree evidence.
- [x] 2.4 Add `reviewing-media-finder-changes` plus metadata, rerun façade-only control decomposition and auxiliary-parser scenarios to GREEN, and prove necessity/subtraction review precedes implementation-correctness review.
- [x] 2.5 Add `evolving-media-finder-contracts` plus metadata, rerun processor `provider_key` and runtime/serialized safe-snapshot scenarios to GREEN, and prove internal names cannot silently change existing JSON/OpenAPI behavior.
- [x] 2.6 Add the version-independent `verifying-and-publishing-media-finder` plus metadata, rerun dirty/changed-HEAD, unavailable-Docker, seven-check, release-target, and GHCR-tag scenarios to GREEN, and prove the procedure derives its target version and tags from approved release context and checked-in metadata.
- [x] 2.7 Add `maintaining-media-finder-skills` plus metadata, rerun the literal-string-validator scenario to GREEN, and prove one skill is baseline-tested, written, rerun, and refined before another skill is changed.

## 3. Strengthen Domain Skills One at a Time

- [x] 3.1 Add a RED metadata-provider pressure case for bounded reads, finite portable payloads, duplicate/unbounded fan-out, and executable/serialized parity; minimally update `adding-metadata-provider` and rerun it to GREEN.
- [x] 3.2 Add a RED release-provider pressure case for canonical safe URL/GUID policy, bounded validation/search/resolve, and logger lifecycle; minimally update `adding-release-provider` and rerun it to GREEN.
- [x] 3.3 Add a RED download-client pressure case for bounded authentication, destination, submission-acknowledgement, and lookup responses while preserving exact correlation and ambiguous timeout handling; minimally update `adding-download-client` and rerun it to GREEN.
- [x] 3.4 Add a RED metadata-schema pressure case for existing wire characterization, stored-version policy, and provider/import-to-API/naming/NFO/UI trace; minimally update `evolving-metadata-schema` and rerun it to GREEN.

## 4. Route and Document the Portable Workflow

- [x] 4.1 Update `AGENTS.md` after every referenced skill exists: retain product/package/security invariants, declare project-local routing, add the architecture complexity circuit breaker and compatibility check, preserve OpenSpec authorization boundaries, avoid duplicating skill bodies, and keep permanent rules independent of a particular release or historical incident.
- [x] 4.2 Update `CONTRIBUTING.md` to link every applicable domain and cross-cutting route, including release-provider and stable-release work, without depending on personal paths or a specific GitHub plugin.
- [x] 4.3 Make the focused skill-catalog architecture test GREEN and verify all manual `SKILL.md` files are concise, trigger-specific, free of absolute workstation paths and hard-coded product-release literals, and paired with valid `agents/openai.yaml`; review the pressure evidence to ensure no skill defines its purpose around one past implementation failure, and do not add a prose-content or LLM evaluation parser.

## 5. Review and Verification Handoff

- [x] 5.1 Run the recorded post-skill scenarios with fresh conversation context, document the exact instruction environment and actual outcomes, distinguish portability/regression evidence from causal evidence, and resolve every Critical or Important behavioral gap before claiming a skill is validated.
- [x] 5.2 Run an independent whole-change review that begins with the pragmatic subtraction pass, checks routing/trigger overlap and OpenSpec consistency, and explicitly rejects new auxiliary platforms, parsers, services, or unproven compatibility machinery.
- [x] 5.3 Run strict OpenSpec validation, documentation-language policy, focused architecture/metadata checks, formatting and diff checks; record proportional local evidence and rely on the existing seven required GitHub contexts for the final pull-request gate.
- [x] 5.4 Present the complete change for specification synchronization and archive with no active implementation task, unresolved review finding, or unrecorded release prerequisite remaining.

## 6. Stable Release Readiness

- [x] 6.1 Verify the checked-in publication skill and documentation define a separate post-merge `v0.2.0` release-preparation branch, exact lockstep version/manifest/hash updates, unchanged API/contract versions, and release notes for the modular schema reset.
- [x] 6.2 Verify the release procedure binds the draft GitHub Release, immutable `v0.2.0` tag, release workflow, `0.2`/`latest` tags, amd64/arm64 manifest, migration warning, and final handoff to exact commit identities without adding a new release service or workflow topology.
