## 1. Extend the existing module SDK contract

- [x] 1.1 Add focused failing tests in `packages/module-sdk/tests/test_schema_artifacts.py`, `test_conformance.py`, and `test_serialized_conformance.py` for optional/default-null plain-text `description`, optional/default-null complete `poster_url`, omitted-field compatibility, invalid URL rejection, matching metadata/conformance schema output, and unchanged public SDK/contract version values and first-party manifest ranges.
- [x] 1.2 Run `UV_CACHE_DIR=/tmp/developer-uv-cache uv run pytest -q packages/module-sdk/tests/test_schema_artifacts.py packages/module-sdk/tests/test_conformance.py packages/module-sdk/tests/test_serialized_conformance.py` and record the expected RED failures before editing production SDK code.
- [x] 1.3 Add the two optional fields to the existing SDK `MetadataSearchResult`, update executable and serialized conformance values as needed, and regenerate `schemas/module-sdk/v1/metadata.schema.json` and `conformance.schema.json` in place with `UV_CACHE_DIR=/tmp/developer-uv-cache uv run media-finder-sdk-schema --output schemas/module-sdk/v1`; do not add a new protocol, capability, version, manifest gate, or compatibility shim.
- [x] 1.4 Re-run the focused SDK tests to GREEN, then run `pnpm module-conformance:test` and `pnpm module-conformance:validate` to prove checked schemas and every serialized module fixture agree.

## 2. Produce previews inside the TMDB module

- [x] 2.1 Add failing movie and series search cases to `packages/modules/metadata-tmdb/tests/test_tmdb_module.py` for non-empty overview, exact complete poster URL construction, missing/empty previews, malformed/traversal paths, and an overlong regex-matching path whose `HttpUrl` construction fails without dropping the result; add a regression in `packages/modules/metadata-manual/tests/test_manual_module.py` proving the production Manual search still emits no previews.
- [x] 2.2 On the host, run `UV_CACHE_DIR=/tmp/developer-uv-cache uv run pytest -q packages/modules/metadata-tmdb/tests/test_tmdb_module.py packages/modules/metadata-manual/tests/test_manual_module.py` and record the expected RED failures before editing either provider.
- [x] 2.3 Update only the TMDB module's search producer to map `overview` and to apply the module-owned `^/[A-Za-z0-9._/-]+$`, no-`..`, `https://image.tmdb.org/t/p/original<path>` poster rule with field-level absence on path or URL-construction failure; update TMDB executable/serialized fixtures as required and leave Manual production behavior unchanged.
- [x] 2.4 Re-run the focused module tests on the host to GREEN and run `pnpm module-conformance:validate` in the sandbox; confirm both media kinds, exact URL values, graceful absence, wheel isolation, and fixture/schema parity.

## 3. Carry previews through core and browser control

- [x] 3.1 Add failing tests to `tests/core/catalog/test_metadata_application_services.py` for defensive revalidation and exact transient retention, including rejection of a nonconforming module result and identity-only selection; to `tests/core/control/test_control_context.py` and `tests/test_control_gateway_metadata.py` for unchanged preview projection; to `tests/test_control_api_metadata.py` for exact values and `null` absence; and to `tests/test_control_openapi.py` for the enriched deterministic contract.
- [x] 3.2 Add `tests/test_control_api_security.py::test_metadata_search_rejections_do_not_invoke_gateway_or_create_selection_state` with a counting gateway and wrong-media-type, missing/invalid CSRF/session, and foreign-Origin cases, proving rejection precedes provider/gateway/cache/token work.
- [x] 3.3 On the host, run `UV_CACHE_DIR=/tmp/developer-uv-cache uv run pytest -q tests/core/catalog/test_metadata_application_services.py tests/core/control/test_control_context.py tests/test_control_contracts.py tests/test_control_gateway_metadata.py tests/test_control_api_metadata.py tests/test_control_api_security.py tests/test_control_openapi.py` and record the expected RED failures before editing production core/control code or snapshots.
- [x] 3.4 Add nullable preview fields to the existing control `MetadataSearchResult`, copy the validated SDK values unchanged in `MetadataControlService`, retain the current cache and identity-only selection semantics, and update fake gateways/fixtures without adding a route, gateway operation, persistence field, URL constructor, or provider branch.
- [x] 3.5 Regenerate `docs/api/control-v1.openapi.json` with `UV_CACHE_DIR=/tmp/developer-uv-cache uv run python scripts/generate-control-openapi.py` and regenerate the bundled client with `pnpm --filter @media-finder/builtin-ui contract:generate`; review that the current `/v1/metadata-searches` response contains nullable `description` and `poster_url` and no unrelated surface change.
- [x] 3.6 Re-run the focused host Python set to GREEN and run `pnpm ui:contract` in the sandbox to prove core/cache, existing-route security, exact JSON/null serialization, OpenAPI, and generated TypeScript parity.

## 4. Correct similarity-confirmation token ownership

- [x] 4.1 Add failing cases to `packages/builtin-ui/web/src/api/control-client.test.ts` and `workflows/metadata-page.test.tsx` proving `kind=similarity` exposes the returned confirmation token, confirmation submits that token exactly once and reaches the saved outcome, and a genuinely expired confirmation still clears stale results and returns to search.
- [x] 4.2 Run `pnpm --filter @media-finder/builtin-ui exec vitest run --config web/vite.config.ts web/src/api/control-client.test.ts web/src/workflows/metadata-page.test.tsx` and record the expected RED failures before changing the client or page.
- [x] 4.3 Update `normalizeFailure` and the page's single selection mutation so the token is explicit mutation input, the returned similarity token owns confirmation, and the backend confirmation model and expiry recovery remain unchanged.
- [x] 4.4 Re-run the focused frontend tests to GREEN; on the host also run `UV_CACHE_DIR=/tmp/developer-uv-cache uv run pytest -q tests/test_control_gateway_metadata.py::test_cross_provider_similarity_requires_new_confirmation_token tests/test_control_conformance_real.py::test_metadata_selection_contract`.

## 5. Replace the result chooser with preview rows

- [x] 5.1 Add failing `MetadataPage` unit cases for provider-grouped poster/description rows, React plain-text rendering, omitted description, missing/error poster fallback, exact complete URL use without reconstruction, `loading="lazy"`, `referrerPolicy="no-referrer"`, row-local localized `Select`, absence of radios/footer save, ordinary and exact-duplicate saved outcomes, recoverable-error re-enable, and exact one-request counts for rapid-repeat and cross-row activation while pending.
- [x] 5.2 Add failing English/Russian accessibility and deterministic browser cases for keyboard selection, visible focus, semantic initiating-row pending/error feedback, similarity and expiry flows, poster failure, and no horizontal overflow at the supported mobile viewport.
- [x] 5.3 Run the focused Vitest result-page tests and `pnpm ui:a11y`, recording the expected RED failures before editing the presentation; run the new Playwright cases on the host and record their expected RED result when their production behavior is not yet present.
- [x] 5.4 Implement the poster-plus-description result row in `MetadataPage` and its presentation styles/localization/fixtures, remove radio and footer-save state, and use the existing shared mutation as a guarded global single-flight owner with pending feedback only on the initiating row and all row actions disabled while pending.
- [x] 5.5 Re-run the focused frontend tests and `pnpm ui:a11y` to GREEN; run `pnpm ui:format`, `pnpm ui:lint`, `pnpm ui:type`, and `pnpm ui:build` in the sandbox, then run `pnpm ui:browser` on the host.

## 6. Verify the exact implementation candidate

- [x] 6.1 Run all focused GREEN checks from sections 1–5 and review the diff for the subtraction boundary: no SDK/version/manifest bump, preview capability/protocol/runtime accessor, sibling/fallback/API-v2 path, compatibility matrix, database or migration change, Manual preview feature, host URL construction, image mediator/proxy/cache, CSP, concurrent-selection protocol, unrelated add/Manual/About/Credits change, or delivery artifact.
- [x] 6.2 In the sandbox run `pnpm spec:validate`, `pnpm docs:check`, `pnpm ui:format`, `pnpm ui:lint`, `pnpm ui:type`, `pnpm ui:test`, `pnpm ui:a11y`, `pnpm ui:contract`, `pnpm ui:build`, `pnpm module-conformance:test`, `pnpm module-conformance:validate`, `UV_CACHE_DIR=/tmp/developer-uv-cache pnpm py:format`, `UV_CACHE_DIR=/tmp/developer-uv-cache pnpm py:lint`, and `UV_CACHE_DIR=/tmp/developer-uv-cache pnpm py:type`.
- [x] 6.3 On the host run `UV_CACHE_DIR=/tmp/developer-uv-cache pnpm py:test` and `pnpm ui:browser`, preserving exact exit status and candidate SHA; report unavailable or failed gates as blocking evidence rather than a pass.
- [x] 6.4 Run `openspec validate --all --strict` and `openspec list --json`, confirm the change remains active with every implementation task checked only after its behavior is present and verified, and stop before canonical sync, archive, commit, PR, merge, or publication pending their separately authorized phases.
