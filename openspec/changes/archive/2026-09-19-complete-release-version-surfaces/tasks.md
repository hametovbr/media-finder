## 1. Tests first

- [x] 1.1 Add failing coverage for the missing surfaces: assert that the preparer's declared version-surface map includes the server's reported build version, and that `BackendControlGateway.build_version` equals the root `VERSION`. Observed RED: adding the gateway to the declared set made `test_prepare_updates_all_lockstep_surfaces_and_only_allowlisted_files` fail with `apps/server/src/media_finder_server/control_gateway.py` reported as an extra expected item, because the preparer did not produce it. The lockstep assertion in `tests/test_workspace_packages.py` was separately observed failing against the real prepared candidate `release-…-v0.5.0-attempt-1`, which carries `build_version = "0.4.0"` while `VERSION` is `0.5.0`.
- [x] 1.2 Add a failing assertion that the prepared tree changes exactly the declared version-derived surfaces, so a surface present in the previous release commit but absent from the map is reported instead of shipped. The declared set now includes the server version module; the assertion is the `set(result["changed_files"]) == expected_changed` comparison and the per-surface version assertions beside it.
- [x] 1.3 Add a failing assertion that `tests/core/acquisition/test_module_runtime_integration.py` derives the expected module version from the first-party composition's own manifests rather than a literal. The suite now compares against `composition.release_manifest.module_version` and `composition.download_manifest.module_version`.
- [x] 1.4 Add failing coverage that `tests/test_prepare_release.py` builds its fixtures from an explicitly requested base version and passes when the checkout it runs in already carries a prepared version. Observed RED: running the suite on the prepared `v0.5.0` candidate reproduced twelve failures, and after the first fix a further hard-coded baseline in the fake lock tool still failed with `owning uv lock regeneration failed with exit code 1`.

## 2. Implement the minimum change

- [x] 2.1 Extend the preparer's version-surface map and transformation to the server's reported build version in `apps/server/src/media_finder_server/control_gateway.py`, keeping every other surface unchanged. A dedicated annotated-default rewriter refuses a tree that has zero or more than one such default.
- [x] 2.2 Change `tests/core/acquisition/test_module_runtime_integration.py` to derive the expected module version from the composition manifests it already constructs.
- [x] 2.3 Change `tests/test_prepare_release.py` so the baseline version is read from the committed tree (`git show HEAD:VERSION`) and the requested version follows from it, instead of both being pinned to the version the repository carried when the suite was written.
- [x] 2.4 Remove superseded version-coupling in the same slice: the fake lock tool's baseline rewrite, the previous-stable tag, the release-notes path, the mutation scripts and every content assertion now derive from the same two constants, so no path still depends on a literal product version.

## 3. Verify

- [x] 3.1 Prepared a candidate locally from `main` with the fixed preparer: it reports **22 changed files**, exactly the surface count of the manual `Release Media Finder v0.4.0` commit `03947f1`, and the server module now reads `build_version: str = "0.5.0"`. Against that freshly prepared tree the previously failing suites pass (`tests/test_prepare_release.py` and `tests/test_workspace_packages.py`: 20 passed; the module-runtime integration suite: 3 passed).
- [x] 3.2 Repository gates on the final candidate, all passing: `ruff format --check .` 325 files; `ruff check .` clean; `mypy` success on 100 files; `pytest` 628 passed; `pnpm delivery:test` 155 passed; `pnpm delivery:validate`; `pnpm docs:check` 481 files; strict `pnpm spec:validate` 10 passed, 0 failed.
- [x] 3.3 Complete and coherent: the preparer's surface set covers every version-derived artifact the previous release commit updated, no test depends on the version the tree happened to carry, and no controller, publisher, workflow or delivery-policy file needed a change.
