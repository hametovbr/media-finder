## 1. Lossless page-owned drafts

- [x] 1.1 Add focused failing behavior tests in `manual-editor.test.tsx`, `manual-document.test.ts` and `manual-add-page.test.tsx` for all four raw lists: trailing delimiters/spaces, blur, clear, mode return, UI-language change and revert-to-initial. Verify request arrays and preservation of untouched comma-containing arrays, rich fields, other-language titles and row-key stripping. Map to Non-destructive Manual list input (RQ-001/TC-001); record actual RED before production changes.
- [x] 1.2 Implement the controlled page-owned raw strings and changed-field-only request projection in the existing Manual page/editor/document owners. Establish initial draft comparison excluding row keys and presentation state, including shared create collection selection; make task 1.1 GREEN without changing DTOs or control-client signatures.

## 2. Operation and file-read safety

- [x] 2.1 Add failing deferred-operation cases in add/edit page tests for rapid click/Enter, competing structured/JSON/CSV/confirmation actions, frozen payload/target, mutation-through-finish exclusion, expired/cancelled confirmation, pending dismissal and stale page callbacks. Map to Exclusive Manual page operations (RQ-002/TC-002); assert exact request counts and retained drafts.
- [x] 2.2 Implement the synchronous per-page admission gate, localized pending feedback, immutable snapshot/consent and mounted-page completion handling across the existing mutations. Keep confirmation tokens ephemeral, disable conflicting controls/dismissal, and make task 2.1 GREEN with no automatic mutation retries.
- [x] 2.3 Add failing file-handler cases for both JSON and CSV: current success/error, oversized file, replacement, clear, direct source editing, mode switch and unmount; include read-versus-submit and mutation-versus-read admission. Implement generation-based stale-result/error exclusion and current-read feedback, then make the cases GREEN. Map to Safe Manual file reads (RQ-002/TC-002); preserve existing server validation and pasted-text byte limits.

## 3. Destructive actions and dirty transitions

- [x] 3.1 Extend editor removal/kind tests to fail before implementation: season with descendants, single episode, populated new series-to-movie, cancellation, exact-target confirmation, safe initial focus and focus restoration/relocation; retain existing locked-identity behavior. Implement deferred destructive intents and dialogs in `ManualEditor`, then verify GREEN. Map to Explicit destructive Manual edits (RQ-003/TC-003).
- [x] 3.2 Add failing add/edit router tests for dirty link/back/forward stay/leave, clean and reverted drafts, inactive JSON/CSV/raw text, shared collection, no mode-switch discard, blocked pending departure and another-item initialization. For an item A-to-B route-parameter transition, assert B's fresh normalized document, collection, raw-list initial buffers, empty CSV source and clean dirty baseline. Implement page-local dirty guards, a single review at a time, keyed edit lifecycle, scoped successful-navigation bypass and best-effort unload registration/cleanup; verify GREEN. Map to Protected Manual draft navigation (RQ-004/TC-004); do not persist drafts or promise OS-termination recovery.
- [x] 3.3 Add failing cases for dirty-form CSV rejection, cancel/confirm structured reset preserving CSV, structured create/edit or JSON submission with another dirty draft, and consent expiry on validation/error/duplicate cancellation. Assert that alternate-draft review blocks underlying edits, mode/file changes, competing mutations and in-app departure, with cancel retaining both drafts and continue admitting only the captured operation. Implement explicit operation-scoped discard review and save-first/reset guidance; verify selected request only, no automatic save-plus-CSV, and both drafts retained after failure. Map to Explicit Manual CSV and alternate-draft consequences (RQ-004/RQ-005/TC-004).

## 4. Presentation and confirmation-test timing

- [x] 4.1 After the page/editor interface is fixed, add failing disclosure/count/preservation tests; implement the single secondary-field group and draft hierarchy counts. Keep basic fields visible and the existing editable hierarchy, preserve raw values and validation state, and make tests GREEN. Map to Accessible Manual secondary-field disclosure (RQ-006/TC-005); no individual season accordion or full hierarchy redesign.
- [x] 4.2 Complete EN/RU labels, safe file/pending messages, destructive counts, alternate-draft consequences and plain-language saved-revision CSV guidance. Cover keyboard access, safe initial focus and returned focus for each new dialog; ensure local action styles permit complete narrow-screen labels. Coordinate locale edits with the primary agent; map to RQ-002 through RQ-006.
- [x] 4.3 Use the retained opacity-0 failure at `manual-add-page.test.tsx:363` as the existing failing observation; replace its immediate visibility assertion with a condition-based visibility wait. Run the focused named test and full UI suite; preserve product animation and visibility expectations. Map to RQ-007/TC-006; do not treat a module/setup error or rerun-until-green as behavioral proof.

## 5. Candidate-bound verification and handoff

- [ ] 5.1 Extend `web/e2e/shell.spec.ts` with scoped Manual scenarios for raw typing, delayed competing submit, destructive cancel/confirm, dirty managed navigation, alternate drafts and CSV gating. Run `pnpm ui:browser` in the authorized local or existing hosted boundary; test discovery alone is not a pass. Use deterministic fixtures and existing report attachments; no real integration calls or workflow changes.
- [ ] 5.2 Capture and inspect Manual secondary fields, destructive review, dirty navigation, alternate-draft review and CSV-blocked guidance in EN/RU at 360x800 and 1280x800. Check complete labels, horizontal overflow, keyboard focus and state preservation; attach screenshot identity/locale/viewport through the existing report and verify run/checkout provenance. Keep existing recovery captures intact. Record unavailable browser evidence as blocking acceptance; map to RQ-006/TC-005/TC-007.
- [ ] 5.3 Run applicable final gates on the same candidate: `pnpm ui:format`, `pnpm ui:lint`, `pnpm ui:type`, `pnpm ui:test`, `pnpm ui:a11y`, `pnpm ui:contract`, `pnpm ui:build`, `pnpm docs:check`, `pnpm spec:validate` and `git diff --check`, plus the browser evidence above. Regenerate packaged assets only through the build and verify no unintended wire/backend/config drift. Record commands, results, counts, SHA and limitations; previous baseline passes do not accept new code.
- [ ] 5.4 Obtain independent scope/behavior review and reconcile findings; record scenario coverage, completed tasks and outstanding gates. Stop apply before archive/publication. After the separately authorized verification/archive phase, synchronize the delta, archive, and follow exact-candidate PR checks/review/merge/main/edge policy; do not mark overall delivery complete at the apply handoff.
